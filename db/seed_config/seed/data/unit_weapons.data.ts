import type { SeedDataset } from "../../types/_index.types";

/**
 * Typed seed dataset for the `unit_weapons` table.
 *
 * Populated by the Wahapedia unit-page importer — not hand-authored.
 */
export const unit_weaponsDataset: SeedDataset<"unit_weapons"> = {
  table: "unit_weapons",
  records: [],
};
