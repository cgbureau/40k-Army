import type { SeedDataset } from "../../types/_index.types";

/**
 * Typed seed dataset for the `weapon_profiles` table.
 *
 * Populated by the Wahapedia unit-page importer — not hand-authored.
 */
export const weapon_profilesDataset: SeedDataset<"weapon_profiles"> = {
  table: "weapon_profiles",
  records: [],
};
